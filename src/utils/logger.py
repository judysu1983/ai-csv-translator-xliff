"""Logger Setup - See conversation for complete implementation"""
import logging

def setup_logger(name, log_file=None, level=logging.INFO):
    # Full implementation in conversation
    logger = logging.getLogger(name)
    return logger
